@model okboba.Web.Models.ProfileViewModel

@{
    ViewBag.Title = "Profile";
    ViewBag.Section = "Profile";
}

@Html.Action("ProfileHeader", new {profileId = Model.ProfileId, isMe = Model.IsMe, section = "Profile" })

@Html.Action("ProfileDetailAndText", new { profileId = Model.ProfileId, isMe = Model.IsMe })

@Html.Partial("~/Views/Photo/_PhotoUpload.cshtml")


@section Scripts {

    <script>
        $(function () {
            //document ready
            $('.js-editinplace-editicon').editinplace();
            $('[data-toggle="photoupload"]').photoUpload();
            $('#detailBasicSection').click(function () {
                $('#detailBasicModal').modal('show');
            });
            $('#detailLifestyleSection').click(function () {
                $('#detailLifestyleModal').modal('show');
            });
            $('#detailJobSection').click(function () {
                $('#detailJobModal').modal('show');
            });
            $('#detailAppearanceSection').click(function () {
                $('#detailAppearanceModal').modal('show');
            });
            $('#detailPersonalitySection').click(function () {
                $('#detailPersonalityModal').modal('show');
            });
        });        
    </script>
}