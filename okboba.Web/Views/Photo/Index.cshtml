@model okboba.Web.Models.PhotoIndexViewModel

@{
    ViewBag.Title = "Profile Photos";
}

@Html.Action("ProfileHeader","Profile", new { profileId = Model.ProfileId, isMe = Model.IsMe, section = "Photos" })

@Html.Action("ListPhotos", new {id = Model.ProfileId, isMe = Model.IsMe })

@Html.Partial("_PhotoUpload")

@section Scripts {

    <script>
        //$('#photoUploadModal').photoUpload();
        //$('#listThumbnails').sortable();
        //$('#testSortable').sortable();
        $('.grid-item').hover(function () {
            $(this).children('.photo-edit-overlay').show();
        });
    </script>
}